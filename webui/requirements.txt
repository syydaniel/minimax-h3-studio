# Versions H3 Studio was tested with (Python 3.13, macOS 27, Apple M3 Max).
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
psutil==7.2.2
# Claude optimizer, used only when ANTHROPIC_API_KEY is set (otherwise the claude CLI)
anthropic==1.5.0
# provides the `hf` command used by scripts/download-weights.sh
huggingface_hub==1.31.0
